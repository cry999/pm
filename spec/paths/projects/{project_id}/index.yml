get:
  operationId: getProject
  tags:
    - 'Project'
  security:
    - bearer: []
  parameters:
    - $ref: '#components/parameters/ProjectID'
  responses:
    '200':
      description: OK
      content:
        'application/json':
          schema:
            $ref: '#components/schemas/Project'
