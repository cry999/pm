schema:
  type: object
  properties:
    name:
      type: string
      example: Example task
    elevator_pitch:
      type: string
      example: This task is for example
  required:
    - name
    - description
